# CRUD_SP
Crear su propia burbuja: Abre la terminal en la carpeta que descargó y crea su entorno vacío:

1. Bash
python -m venv .venv

2. Bash
.\.venv\Scripts\activate

3. Bash
pip install -r requirements.txt

4. Bash
uvicorn main:app --reload
